# Rehab mobile device binding public-config hotfix - 2026-07-08

## Why

Browser QA showed that the live Device page still behaves as a static demo:

- `window.RehabArmBluetoothBridge` is not present.
- `window.Capacitor.Plugins.RehabArmBluetooth` is not present.
- Clicking `开始搜索设备` renders the hard-coded demo device `领动康复手臂 v2`.
- No unavailable/missing-bridge state is rendered.

The live `GET /api/rehab-arm/app/v1/public-config` previously reported
`PHONE_NATIVE_BLUETOOTH_BRIDGE = debug_bridge_available`, which overstated the
current app release.

## Server Change

Patched the live server file:

`/home/ubuntu/apps/ai-collab/apps/api/app/modules/rehab_arm/app_router.py`

Backup created on the server:

`/home/ubuntu/apps/ai-collab/apps/api/app/modules/rehab_arm/app_router.py.bak-device-binding-static-proof-20260708`

The patch was generated by:

```powershell
cloud\rehab-platform\.venv\Scripts\python.exe tools\patch_rehab_cloud_public_config_device_binding.py `
  --input artifacts\rehab-cloud-hotfix\app_router.remote-before-20260708.py `
  --output artifacts\rehab-cloud-hotfix\app_router.remote-after-20260708.py
```

The API was restarted with the original deployment metadata:

- `AI_COLLAB_BUILD_SHA=ba132ecb70d701ce1233f145208021c75d0091f9`
- `AI_COLLAB_BUILD_REF=app/rehab-arm-mobile-stitch`
- `AI_COLLAB_BUILD_TIME=2026-07-07T16:14:58Z`

## Verified Result

`GET http://106.55.62.122:8011/health`

- `status=ok`
- `pid=3241659`
- `build_sha=ba132ecb70d701ce1233f145208021c75d0091f9`

`GET http://106.55.62.122:8011/api/rehab-arm/app/v1/public-config`

- `data.device_binding.native_bluetooth_bridge.status=missing_in_current_apk`
- `data.device_binding.web_fallback=show_unavailable_state_do_not_fake_devices`
- `APK_FRONTEND_API_WIRING=partial_static_device_binding_blocked`
- `PHONE_NATIVE_BLUETOOTH_BRIDGE=missing_in_current_apk`

## Remaining Blocker

Stitch MCP currently returns `401 invalid authentication credentials`, so the
Profile phone-binding UI and Device missing-bridge UI cannot be regenerated from
Stitch yet. Do not hand-edit the frontend pages outside Stitch.
