# 运行应用到模拟器

## ✅ 当前状态

- ✅ Android Studio 已打开项目
- ✅ Gradle 同步成功
- ✅ 模拟器已启动 (emulator-5554)
- ✅ 项目代码已准备就绪

## 🚀 在Android Studio中运行应用

### 方法1：使用Run按钮（推荐）

1. **查看顶部工具栏**
   - 找到设备选择器（应该显示 "Medium_Phone_API_36.1" 或 "emulator-5554"）
   - 如果没有自动选择，点击下拉菜单选择模拟器

2. **点击Run按钮**
   - 点击绿色的三角形 ▶️ 按钮
   - 或按快捷键 **Shift + F10**

3. **等待编译和安装**
   - 底部会显示构建进度
   - 首次编译需要几分钟
   - 完成后应用会自动安装到模拟器

### 方法2：使用菜单

1. 点击菜单 **Run → Run 'app'**
2. 选择目标设备（模拟器）
3. 等待编译和安装

### 方法3：使用命令行（备选）

如果Android Studio运行失败，可以使用命令行：

```bash
cd /home/wen/RehabRobotArm

# 编译APK
./gradlew assembleDebug

# 安装到模拟器
adb install -r app/build/outputs/apk/debug/app-debug.apk

# 启动应用
adb shell am start -n com.rehab.robotarm/.MainActivity
```

## 📱 应用运行后

### 你会看到：

1. **底部导航栏** - 4个标签
   - 机械臂控制
   - 传感器数据
   - 康复分析
   - 模式控制

2. **默认界面** - 机械臂控制
   - 连接状态卡片（显示"未连接"）
   - 机械臂2D可视化
   - 温度图例

### 测试功能：

1. **切换界面**
   - 点击底部导航栏的不同标签
   - 查看各个界面的内容

2. **查看传感器数据**
   - 点击"传感器数据"标签
   - 查看模拟的传感器数值

3. **查看康复分析**
   - 点击"康复分析"标签
   - 查看AI评分和图表

4. **切换模式**
   - 点击"模式控制"标签
   - 尝试切换不同模式
   - 在被动模式下，机械臂控制界面会显示滑块

## 🔧 如果遇到问题

### 编译错误

查看底部的 "Build" 窗口，检查错误信息。

### 应用闪退

1. 打开 **Logcat** 窗口（底部）
2. 过滤包名：`com.rehab.robotarm`
3. 查看错误日志

### 权限问题

首次运行时，应用会请求权限：
- 蓝牙权限
- 位置权限
- 网络权限

请全部允许。

### 模拟器卡顿

如果模拟器运行缓慢：
1. 关闭其他应用释放内存
2. 或在Android Studio中选择性能更好的模拟器配置

## 📸 预期效果截图说明

### 界面1：机械臂控制
- 顶部显示"机械臂控制"标题
- 连接状态卡片（红色背景，显示"未连接"）
- 中间是机械臂可视化区域（灰色基座，两个关节）
- 底部是温度图例（蓝绿橙红）

### 界面2：传感器数据
- 多个卡片显示不同类型的传感器
- 角度传感器、力传感器、加速度传感器、温度传感器
- 每个卡片内有多行数据

### 界面3：康复分析
- 顶部是综合评分大卡片
- 详细指标进度条
- 运动趋势图表
- AI建议文本

### 界面4：模式控制
- 当前模式显示卡片
- 三个模式切换按钮
- 模式说明卡片

## 🎯 下一步

应用运行成功后，你可以：

1. **测试UI交互** - 点击各个按钮和滑块
2. **查看模拟数据** - 观察数据显示是否正常
3. **测试蓝牙连接** - 使用真实Android设备连接Python模拟器
4. **修改代码** - 根据需求自定义功能

## 💡 提示

- 应用使用模拟数据，无需连接真实设备即可查看所有界面
- 蓝牙功能需要真实Android设备测试（模拟器不支持蓝牙）
- 修改代码后，点击 Run 按钮重新运行即可看到效果

---

**现在请在Android Studio中点击绿色的Run按钮！** ▶️
